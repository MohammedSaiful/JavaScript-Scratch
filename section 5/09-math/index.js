// Math.random() returns a floating-point number between 0 (inclusive) and 1 (exclusive).
// Get a random number between 0 and 10
let random = Math.floor(Math.random() * 11);
console.log(random);


// Math.max(1, 2, 3): Returns the largest number (3).

// Math.min(1, 2, 3): Returns the smallest number (1).

// Math.pow(2, 3): Returns 2 to the power of 3 ($2^3 = 8$).

// Math.sqrt(16): Returns the square root ($\sqrt{16} = 4$).

// Math.abs(-5): Returns the absolute value (5).